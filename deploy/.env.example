# Salin ke /etc/ipnu-sso/deploy.env pada VPS, atau gunakan DEPLOY_ENV_FILE.
# Hanya alamat service lokal; domain publik/eksternal tidak diubah.
BACKEND_UPSTREAM_URL=http://127.0.0.1:8180
FRONTEND_UPSTREAM_URL=http://127.0.0.1:3100
